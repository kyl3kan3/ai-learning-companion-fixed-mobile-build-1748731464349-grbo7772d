import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aiappbuilder.ailearningcompanionfixedmobilebuild1748731464349grbo7772d',
  appName: 'ai-learning-companion-fixed',
  webDir: 'dist',
  bundledWebRuntime: false,
  server: {
    androidScheme: 'https'
  }
};

export default config;